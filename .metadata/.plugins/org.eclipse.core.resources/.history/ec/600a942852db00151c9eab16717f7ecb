package br.com.tecnonoticias.testeLaboratorio.testeLaboratorio;

import java.io.BufferedReader;
import java.io.File;
import java.io.FileNotFoundException;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;
import java.util.StringTokenizer;
import java.util.logging.Level;
import java.util.logging.Logger;

public class Cc52259769 {

	public Cc52259769() {

	}

	public void registraNiveisHormonio(String path) throws IOException {

		File fl = new File(path);
		FileWriter fw = new FileWriter(fl);
		// int line = (int) (Math.random() * 100000);
		int read = 0;
		// for (int j = 0; j <= line; j++) {
		read = (int) (Math.random() * 100);
		for (int i = 0; i <= read; i++) {
			fw.write((int) (Math.random() * 100) + " ");
		}
		fw.write("\n");
		// }

		fw.close();

	}

	public static void main(String args[]) {

		// Cc52259769 in = new Cc52259769();
		try {
			// new leituraCoor().lendoArquivo();
			new lendoLogHorm().LendoArquivoLoghorm();
			// in.registraNiveisHormonio("logHorm");
			// in.writeFileOutput("coor2");
			// in.writeFileOutput("coor3");
		} catch (IOException ex) {
			Logger.getLogger(Cc52259769.class.getName()).log(Level.SEVERE, null, ex);
		}

	}

}

class leituraCoor {

	public void lendoArquivo() throws IOException {
		BufferedReader in1 = new BufferedReader(
				new FileReader("/media/edgar/Novo volume/faculdade/algoritmoalvorada-master/coor"));
		BufferedReader in = new BufferedReader(new FileReader(
				"/media/edgar/Novo volume/faculdade/AlgoritimoLogicaProgramacaoIV/testeLaboratorio/coor"));

		String text = in1.readLine();
		in1.close();

		StringTokenizer coor = new StringTokenizer(text, " ");
		String[] arrayCoor = new String[coor.countTokens()];

		/* linha */
		for (int i = 0; i < coor.countTokens(); i++) {
			/* quantidade de numeros na linha */
			// for (int j = 0; j < 5; j++) {

			arrayCoor[i] = coor.nextToken();

			System.out.println("Posições " + i + " " + arrayCoor[i]);

			// }
		}
	}
}

class lendoLogHorm {

	public void LendoArquivoLoghorm() throws IOException {
		BufferedReader logHorm1 = new BufferedReader(
				new FileReader("/media/edgar/Novo volume/faculdade/algoritmoalvorada-master/logHorm1"));
		String logHormTeste = logHorm1.readLine();
		logHorm1.close();
		
		System.out.println(logHormTeste.length());

		BufferedReader logHorm = new BufferedReader(new FileReader(
				"/media/edgar/Novo volume/faculdade/AlgoritimoLogicaProgramacaoIV/testeLaboratorio/logHorm"));
		String text = logHorm.readLine();
		logHorm.close();

//		String[] arrayLoghorm = new String[100];

		/* linha */
		for (int i = 0; i < 100000; i++) {
			/* quantidade de numeros na linha */
			 for (int j = 0; j < 100; j++) {

			String[] arrayLoghorm = text.split(" ");

			System.out.println("Posições " + i + " " + arrayLoghorm[i]);

			 }
		}
	}
}